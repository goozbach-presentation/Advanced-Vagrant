127.0.0.1   {{ ansible_fqdn }} {{ ansible_hostname }} localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6
{% for myhost in vagrant_hosts %}
{% if myhost.name != ansible_hostname %}
{{ myhost.address }}	{{ myhost.hostname }} {{ myhost.name }}
{% else %}
{% endif %}
{% else %}
# no loop in myhost
{% endfor %}
